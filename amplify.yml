version: 1
backend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
