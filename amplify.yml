version: 1
backend:
  phases:
    preBuild:
      commands:
        - echo "Install pipenv"
        - pip install pipenv
    build:
      commands:
        - echo "Install dependencies with pipenv"
        - pipenv install
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
