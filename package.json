{
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.533.0",
    "@aws-sdk/lib-dynamodb": "^3.533.0",
    "aws-amplify": "^6.0.13"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
